// This is a text-based architecture diagram

┌─────────────────────────────────────────────────────────────────────┐
│ END-TO-END REQUEST FLOW │
│ Gameplan Scheduler Application │
└─────────────────────────────────────────────────────────────────────┘

👤 USER (Browser)
│
│ 1. User opens URL or clicks a link
│ (e.g., http://localhost/gameplan-scheduler/)
│
▼
┌──────────────────────────────────────┐
│ 🌐 BROWSER (Client Side) │
│ │
│ ┌────────┐ ┌────────┐ ┌──────────┐ │
│ │ HTML │ │ CSS / │ │JavaScript│ │
│ │Structure│ │Bootstrap│ │ Logic │ │
│ └────────┘ └────────┘ └──────────┘ │
│ │
│ • HTML renders the page structure │
│ • Bootstrap styles the UI │
│ • JavaScript handles interactivity │
│ • JavaScript validates forms │
│ │
└──────────────┬───────────────────────┘
│
│ 2. HTTP REQUEST (GET/POST)
│ User submits a form or
│ requests a page
│
▼
┌──────────────────────────────────────┐
│ 🖥️ APACHE WEB SERVER (XAMPP) │
│ │
│ • Receives the HTTP request │
│ • Routes to the correct .php file │
│ • Passes request to PHP engine │
│ │
└──────────────┬───────────────────────┘
│
│ 3. PHP PROCESSING
│
▼
┌──────────────────────────────────────┐
│ ⚙️ PHP ENGINE (Server Side) │
│ │
│ • Reads form data ($\_POST, $\_GET) │
│ • Validates input on server side │
│ • Executes business logic │
│ • Queries the database │
│ • Generates dynamic HTML │
│ │
└──────────────┬───────────────────────┘
│
│ 4. DATABASE QUERY (SQL)
│
▼
┌──────────────────────────────────────┐
│ 🗄️ MySQL DATABASE (XAMPP) │
│ │
│ • Stores game plans, schedules, │
│ user data │
│ • Executes SELECT, INSERT, UPDATE, │
│ DELETE queries │
│ • Returns result set to PHP │
│ │
└──────────────┬───────────────────────┘
│
│ 5. QUERY RESULTS returned to PHP
│
▼
┌──────────────────────────────────────┐
│ ⚙️ PHP ENGINE │
│ │
│ • Processes database results │
│ • Builds HTML page with data │
│ • Sends complete HTML response │
│ │
└──────────────┬───────────────────────┘
│
│ 6. HTTP RESPONSE (HTML + CSS + JS)
│
▼
┌──────────────────────────────────────┐
│ 🌐 BROWSER (Client Side) │
│ │
│ • Receives HTML response │
│ • Renders the page (HTML) │
│ • Applies styles (Bootstrap/CSS) │
│ • Executes JavaScript │
│ • Page displayed to user ✅ │
│ │
└──────────────────────────────────────┘
│
▼
👤 USER sees the updated page
