# 🏆 GamePlan Scheduler

## Architecture & End-to-End Request Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        GAMEPLAN SCHEDULER — FULL ARCHITECTURE               │
└─────────────────────────────────────────────────────────────────────────────┘

 ┌──────────┐        ┌──────────────────┐        ┌──────────────────┐
 │  👤 USER │        │   🌐 BROWSER     │        │  🖥️ APACHE SERVER│
 │          │───────▶│  (Client-Side)   │───────▶│   (Server-Side)  │
 │          │        │                  │        │                  │
 └──────────┘        └──────────────────┘        └──────────────────┘
                              │                          │
                              ▼                          ▼
                     ┌────────────────┐         ┌────────────────┐
                     │  FRONTEND TECH │         │  BACKEND TECH  │
                     ├────────────────┤         ├────────────────┤
                     │ • HTML         │         │ • PHP 8.x      │
                     │ • CSS          │         │ • Sessions      │
                     │ • Bootstrap 5  │         │ • Form Handling │
                     │ • JavaScript   │         │ • Validation    │
                     └────────────────┘         └───────┬────────┘
                                                        │
                                                        ▼
                                                ┌────────────────┐
                                                │  🗄️ DATABASE   │
                                                │   MySQL/Maria  │
                                                │   (via XAMPP)  │
                                                ├────────────────┤
                                                │ • Users Table  │
                                                │ • Schedules    │
                                                │ • Game Plans   │
                                                └────────────────┘
```

---

## 🔄 Detailed End-to-End Request Flow

```
STEP 1 — USER ACTION
══════════════════════════════════════════════════════════════
│  User opens browser and navigates to:                     │
│  http://localhost/gameplan-scheduler/                      │
│                                                           │
│  📌 Technology: Browser (Chrome, Firefox, Edge)           │
══════════════════════════════════════════════════════════════
                         │
                         ▼
STEP 2 — HTTP REQUEST
══════════════════════════════════════════════════════════════
│  Browser sends HTTP GET/POST request to Apache server     │
│                                                           │
│  GET  /index.php          → Load page                     │
│  POST /schedule.php       → Submit form data              │
│                                                           │
│  📌 Technology: HTTP/HTTPS Protocol                       │
══════════════════════════════════════════════════════════════
                         │
                         ▼
STEP 3 — APACHE SERVER RECEIVES REQUEST
══════════════════════════════════════════════════════════════
│  Apache (XAMPP) receives the request and routes it        │
│  to the appropriate PHP file for processing.              │
│                                                           │
│  📌 Technology: Apache Web Server (XAMPP)                 │
══════════════════════════════════════════════════════════════
                         │
                         ▼
STEP 4 — PHP PROCESSES SERVER-SIDE LOGIC
══════════════════════════════════════════════════════════════
│  PHP interpreter executes the script:                     │
│                                                           │
│  ✅ Validates user input (sanitization, type checking)    │
│  ✅ Manages sessions (login state, user identity)         │
│  ✅ Builds SQL queries for database operations            │
│  ✅ Handles business logic (scheduling rules, conflicts)  │
│                                                           │
│  📌 Technology: PHP 8.x                                   │
══════════════════════════════════════════════════════════════
                         │
                         ▼
STEP 5 — DATABASE INTERACTION
══════════════════════════════════════════════════════════════
│  PHP connects to MySQL database via MySQLi/PDO            │
│                                                           │
│  📖 SELECT → Retrieve schedules, user data                │
│  📝 INSERT → Create new game plans, register users        │
│  ✏️  UPDATE → Modify existing schedules                    │
│  🗑️  DELETE → Remove cancelled events                      │
│                                                           │
│  📌 Technology: MySQL / MariaDB (XAMPP phpMyAdmin)        │
══════════════════════════════════════════════════════════════
                         │
                         ▼
STEP 6 — PHP GENERATES HTML RESPONSE
══════════════════════════════════════════════════════════════
│  PHP dynamically builds the HTML page with data           │
│  retrieved from the database and sends it back.           │
│                                                           │
│  📌 Technology: PHP + HTML                                │
══════════════════════════════════════════════════════════════
                         │
                         ▼
STEP 7 — BROWSER RENDERS THE PAGE
══════════════════════════════════════════════════════════════
│  Browser receives HTML and renders it:                    │
│                                                           │
│  1️⃣  HTML    → Builds the DOM (page structure)            │
│  2️⃣  CSS     → Applies styles and layout                  │
│  3️⃣  Bootstrap → Responsive grid, components, utilities   │
│  4️⃣  JavaScript → Adds interactivity and validation       │
│                                                           │
│  📌 Technology: HTML5, CSS3, Bootstrap 5, JavaScript ES6  │
══════════════════════════════════════════════════════════════
                         │
                         ▼
STEP 8 — USER SEES THE FINAL PAGE
══════════════════════════════════════════════════════════════
│  ✅ Fully rendered, interactive, responsive web page      │
│  ✅ User can interact → triggers new requests (back to 1) │
│                                                           │
│  📌 Technology: Browser Rendering Engine                  │
══════════════════════════════════════════════════════════════
```

---

## 🛠️ Technology Stack Summary

| Layer             | Technology         | Purpose                                                |
| ----------------- | ------------------ | ------------------------------------------------------ |
| **Structure**     | HTML5              | Page structure and semantic markup                     |
| **Styling**       | CSS3 + Bootstrap 5 | Visual design, responsive layout, pre-built components |
| **Interactivity** | JavaScript (ES6)   | Client-side validation, DOM manipulation, dynamic UI   |
| **Server Logic**  | PHP 8.x            | Backend processing, form handling, session management  |
| **Database**      | MySQL / MariaDB    | Persistent data storage (users, schedules, game plans) |
| **Web Server**    | Apache (XAMPP)     | Serves files, routes requests, hosts the application   |

---

## 📊 Component Interaction Map

```
┌─────────────────────────────────────────────────┐
│                 FRONTEND (Browser)               │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │  HTML5   │  │   CSS3   │  │  JavaScript   │  │
│  │ Structure│  │ + Boot-  │  │ Interactivity │  │
│  │  & Forms │  │  strap 5 │  │ & Validation  │  │
│  └────┬─────┘  └────┬─────┘  └──────┬────────┘  │
│       │              │               │           │
│       └──────────────┼───────────────┘           │
│                      │                           │
└──────────────────────┼───────────────────────────┘
                       │ HTTP Request / Response
                       ▼
┌──────────────────────┼───────────────────────────┐
│              BACKEND (XAMPP Server)               │
│                      │                           │
│              ┌───────▼────────┐                  │
│              │   Apache Web   │                  │
│              │    Server      │                  │
│              └───────┬────────┘                  │
│                      │                           │
│              ┌───────▼────────┐                  │
│              │    PHP 8.x     │                  │
│              │ Server Logic   │                  │
│              │ Sessions       │                  │
│              │ Validation     │                  │
│              └───────┬────────┘                  │
│                      │ SQL Queries               │
│              ┌───────▼────────┐                  │
│              │     MySQL      │                  │
│              │   Database     │                  │
│              │  (MariaDB)     │                  │
│              └────────────────┘                  │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## 🚀 How to Run

1. Start **XAMPP** (Apache + MySQL)
2. Place project in `C:\xampp\htdocs\gameplan-scheduler\`
3. Import the database via **phpMyAdmin**
4. Navigate to `http://localhost/gameplan-scheduler/`

---

© 2026 GamePlan Scheduler — Built with PHP, MySQL, HTML, CSS, Bootstrap & JavaScript
